import React from "react";
import RegisterComponent from "../components/UI/RegisterComponent";

const NewAgent = () => {
    return <RegisterComponent title="Become new agent" type={1} />;
};

export default NewAgent;
